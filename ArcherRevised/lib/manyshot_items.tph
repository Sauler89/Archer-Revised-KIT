// Archer Revised - item patcher for Manyshot.
//
// The core component runs this once for every bow/crossbow already present.
// Component 10 can run the same patch near the end of an install order so
// bows/crossbows added by later mods receive the same effects.
//
// Effects are deliberately idempotent: Archer Revised effects are removed
// before being re-added. Opcode 177 filters directly for KIT.IDS = FERALAN,
// so no custom SPLPROT row or auxiliary immunity effects are required.

COPY_EXISTING_REGEXP GLOB ~.*\.itm~ ~override~
  PATCH_IF (SOURCE_SIZE > 0x71) BEGIN
    READ_SHORT 0x1c item_type

    PATCH_IF (item_type = 15 OR item_type = 27) BEGIN
      // Remove only effects owned by Archer Revised. Also remove the obsolete
      // opcode-318 effects used by an earlier development implementation.
      LPF DELETE_EFFECT INT_VAR match_opcode = 177 STR_VAR match_resource = ~ARMSH07~ END
      LPF DELETE_EFFECT INT_VAR match_opcode = 177 STR_VAR match_resource = ~ARMSH13~ END
      LPF DELETE_EFFECT INT_VAR match_opcode = 177 STR_VAR match_resource = ~ARMSH20~ END
      LPF DELETE_EFFECT INT_VAR match_opcode = 318 STR_VAR match_resource = ~ARMSH07~ END
      LPF DELETE_EFFECT INT_VAR match_opcode = 318 STR_VAR match_resource = ~ARMSH13~ END
      LPF DELETE_EFFECT INT_VAR match_opcode = 318 STR_VAR match_resource = ~ARMSH20~ END

      // Apply each EFF only to the vanilla Archer kit (FERALAN).
      // Opcode 177 parameter2 = 9 uses KIT.IDS for the parameter1 selector.
      // The EFF itself carries the minimum affected level (7 / 13 / 20).
      LPF ADD_ITEM_EQEFFECT
        INT_VAR
          silent         = 1
          opcode         = 177
          target         = 1
          parameter1     = IDS_OF_SYMBOL (~KIT~ ~FERALAN~)
          parameter2     = 9
          timing         = 2
        STR_VAR
          resource       = ~ARMSH07~
      END
      LPF ADD_ITEM_EQEFFECT
        INT_VAR
          silent         = 1
          opcode         = 177
          target         = 1
          parameter1     = IDS_OF_SYMBOL (~KIT~ ~FERALAN~)
          parameter2     = 9
          timing         = 2
        STR_VAR
          resource       = ~ARMSH13~
      END
      LPF ADD_ITEM_EQEFFECT
        INT_VAR
          silent         = 1
          opcode         = 177
          target         = 1
          parameter1     = IDS_OF_SYMBOL (~KIT~ ~FERALAN~)
          parameter2     = 9
          timing         = 2
        STR_VAR
          resource       = ~ARMSH20~
      END
    END
  END
BUT_ONLY_IF_IT_CHANGES
